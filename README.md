# symfony-ldap-skeleton


1. Setup database connection in .env file

DATABASE_URL="mysql://db_user:db_password@127.0.0.1:3306/db_name?serverVersion=5.7"

2. Run composer update to instal packages

composer update
